name: simulador_so
description: Simulador de Planificación de Procesos - FCFS & SPN
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'  # Cambia esto

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  csv: ^5.1.1
  google_fonts: ^6.1.0
  fl_chart: ^0.68.0
  provider: ^6.1.1
  intl: ^0.19.0
  path_provider: ^2.1.2
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/
